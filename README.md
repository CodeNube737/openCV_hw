New edit to make the width and height of the gui variables (use cv::size() ). 
also, edit to add a circle, a square, a triangle, and a curvy line to the gui: each in its own corner, a set distance from the edge of the gui.
